class TodoModel {
  int id;
  String title;
  String body;
  DateTime createdAt;

  TodoModel({
    required this.id,
    required this.title,
    required this.body,
    required this.createdAt,
  });
}
